import { log, reply, addr, args } from '../utils/common.js';
import thumbnail from '../utils/thumbnail.js';

const post = {
  what: () => {
    console.log('Hello World!');
  }
}

export default post;